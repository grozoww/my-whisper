# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Editor / LSP: contains absolute machine-specific paths, regenerate with
#   xcode-build-server config -project OurWhisper.xcodeproj -scheme OurWhisper
buildServer.json

# Swift Package Manager
.build/
.swiftpm/
Package.resolved.bak

# Release artifacts
dist/
*.dmg
*.zip

# Secrets — never commit. API keys live in the user's Keychain at runtime.
*.p12
*.cer
*.provisionprofile
.env
.env.*
